description: |
  This branch is just a dummy adapter branch
  so that bench-runner can run the watermark pool in the same way.

algorithm_parameters: 
  - description: minimum pool size
    name: min_size
  - description: maximum pool size
    name: max_size
  - description: timeout for worker threads in milliseconds
    name: timeout
algorithm_parameter_combos:
  - [1, 64, 1000]
  - [1, 64, 500]
  - [1, 32, 1000]
  - [1, 32, 500]
